export default function ProfilePage() {

    let throwError  = true;

    if (throwError){
        throw new Error("Profile ID is missing!"); // Simulated error
    }

    
  return (
    <>
    
    </>
  );
}
